// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sf/firehose/v2/firehose.proto

package pbfirehoseconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v2 "github.com/streamingfast/pbgo/sf/firehose/v2"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StreamName is the fully-qualified name of the Stream service.
	StreamName = "sf.firehose.v2.Stream"
	// FetchName is the fully-qualified name of the Fetch service.
	FetchName = "sf.firehose.v2.Fetch"
	// EndpointInfoName is the fully-qualified name of the EndpointInfo service.
	EndpointInfoName = "sf.firehose.v2.EndpointInfo"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StreamBlocksProcedure is the fully-qualified name of the Stream's Blocks RPC.
	StreamBlocksProcedure = "/sf.firehose.v2.Stream/Blocks"
	// FetchBlockProcedure is the fully-qualified name of the Fetch's Block RPC.
	FetchBlockProcedure = "/sf.firehose.v2.Fetch/Block"
	// EndpointInfoInfoProcedure is the fully-qualified name of the EndpointInfo's Info RPC.
	EndpointInfoInfoProcedure = "/sf.firehose.v2.EndpointInfo/Info"
)

// StreamClient is a client for the sf.firehose.v2.Stream service.
type StreamClient interface {
	Blocks(context.Context, *connect.Request[v2.Request]) (*connect.ServerStreamForClient[v2.Response], error)
}

// NewStreamClient constructs a client for the sf.firehose.v2.Stream service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStreamClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StreamClient {
	baseURL = strings.TrimRight(baseURL, "/")
	streamMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("Stream").Methods()
	return &streamClient{
		blocks: connect.NewClient[v2.Request, v2.Response](
			httpClient,
			baseURL+StreamBlocksProcedure,
			connect.WithSchema(streamMethods.ByName("Blocks")),
			connect.WithClientOptions(opts...),
		),
	}
}

// streamClient implements StreamClient.
type streamClient struct {
	blocks *connect.Client[v2.Request, v2.Response]
}

// Blocks calls sf.firehose.v2.Stream.Blocks.
func (c *streamClient) Blocks(ctx context.Context, req *connect.Request[v2.Request]) (*connect.ServerStreamForClient[v2.Response], error) {
	return c.blocks.CallServerStream(ctx, req)
}

// StreamHandler is an implementation of the sf.firehose.v2.Stream service.
type StreamHandler interface {
	Blocks(context.Context, *connect.Request[v2.Request], *connect.ServerStream[v2.Response]) error
}

// NewStreamHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStreamHandler(svc StreamHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	streamMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("Stream").Methods()
	streamBlocksHandler := connect.NewServerStreamHandler(
		StreamBlocksProcedure,
		svc.Blocks,
		connect.WithSchema(streamMethods.ByName("Blocks")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.firehose.v2.Stream/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StreamBlocksProcedure:
			streamBlocksHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStreamHandler returns CodeUnimplemented from all methods.
type UnimplementedStreamHandler struct{}

func (UnimplementedStreamHandler) Blocks(context.Context, *connect.Request[v2.Request], *connect.ServerStream[v2.Response]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("sf.firehose.v2.Stream.Blocks is not implemented"))
}

// FetchClient is a client for the sf.firehose.v2.Fetch service.
type FetchClient interface {
	Block(context.Context, *connect.Request[v2.SingleBlockRequest]) (*connect.Response[v2.SingleBlockResponse], error)
}

// NewFetchClient constructs a client for the sf.firehose.v2.Fetch service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFetchClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FetchClient {
	baseURL = strings.TrimRight(baseURL, "/")
	fetchMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("Fetch").Methods()
	return &fetchClient{
		block: connect.NewClient[v2.SingleBlockRequest, v2.SingleBlockResponse](
			httpClient,
			baseURL+FetchBlockProcedure,
			connect.WithSchema(fetchMethods.ByName("Block")),
			connect.WithClientOptions(opts...),
		),
	}
}

// fetchClient implements FetchClient.
type fetchClient struct {
	block *connect.Client[v2.SingleBlockRequest, v2.SingleBlockResponse]
}

// Block calls sf.firehose.v2.Fetch.Block.
func (c *fetchClient) Block(ctx context.Context, req *connect.Request[v2.SingleBlockRequest]) (*connect.Response[v2.SingleBlockResponse], error) {
	return c.block.CallUnary(ctx, req)
}

// FetchHandler is an implementation of the sf.firehose.v2.Fetch service.
type FetchHandler interface {
	Block(context.Context, *connect.Request[v2.SingleBlockRequest]) (*connect.Response[v2.SingleBlockResponse], error)
}

// NewFetchHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFetchHandler(svc FetchHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	fetchMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("Fetch").Methods()
	fetchBlockHandler := connect.NewUnaryHandler(
		FetchBlockProcedure,
		svc.Block,
		connect.WithSchema(fetchMethods.ByName("Block")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.firehose.v2.Fetch/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FetchBlockProcedure:
			fetchBlockHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFetchHandler returns CodeUnimplemented from all methods.
type UnimplementedFetchHandler struct{}

func (UnimplementedFetchHandler) Block(context.Context, *connect.Request[v2.SingleBlockRequest]) (*connect.Response[v2.SingleBlockResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.firehose.v2.Fetch.Block is not implemented"))
}

// EndpointInfoClient is a client for the sf.firehose.v2.EndpointInfo service.
type EndpointInfoClient interface {
	Info(context.Context, *connect.Request[v2.InfoRequest]) (*connect.Response[v2.InfoResponse], error)
}

// NewEndpointInfoClient constructs a client for the sf.firehose.v2.EndpointInfo service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEndpointInfoClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EndpointInfoClient {
	baseURL = strings.TrimRight(baseURL, "/")
	endpointInfoMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("EndpointInfo").Methods()
	return &endpointInfoClient{
		info: connect.NewClient[v2.InfoRequest, v2.InfoResponse](
			httpClient,
			baseURL+EndpointInfoInfoProcedure,
			connect.WithSchema(endpointInfoMethods.ByName("Info")),
			connect.WithClientOptions(opts...),
		),
	}
}

// endpointInfoClient implements EndpointInfoClient.
type endpointInfoClient struct {
	info *connect.Client[v2.InfoRequest, v2.InfoResponse]
}

// Info calls sf.firehose.v2.EndpointInfo.Info.
func (c *endpointInfoClient) Info(ctx context.Context, req *connect.Request[v2.InfoRequest]) (*connect.Response[v2.InfoResponse], error) {
	return c.info.CallUnary(ctx, req)
}

// EndpointInfoHandler is an implementation of the sf.firehose.v2.EndpointInfo service.
type EndpointInfoHandler interface {
	Info(context.Context, *connect.Request[v2.InfoRequest]) (*connect.Response[v2.InfoResponse], error)
}

// NewEndpointInfoHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEndpointInfoHandler(svc EndpointInfoHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	endpointInfoMethods := v2.File_sf_firehose_v2_firehose_proto.Services().ByName("EndpointInfo").Methods()
	endpointInfoInfoHandler := connect.NewUnaryHandler(
		EndpointInfoInfoProcedure,
		svc.Info,
		connect.WithSchema(endpointInfoMethods.ByName("Info")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.firehose.v2.EndpointInfo/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EndpointInfoInfoProcedure:
			endpointInfoInfoHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEndpointInfoHandler returns CodeUnimplemented from all methods.
type UnimplementedEndpointInfoHandler struct{}

func (UnimplementedEndpointInfoHandler) Info(context.Context, *connect.Request[v2.InfoRequest]) (*connect.Response[v2.InfoResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.firehose.v2.EndpointInfo.Info is not implemented"))
}
