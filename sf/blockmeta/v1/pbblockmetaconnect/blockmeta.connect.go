// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sf/blockmeta/v1/blockmeta.proto

package pbblockmetaconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/streamingfast/pbgo/sf/blockmeta/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TimeToIDName is the fully-qualified name of the TimeToID service.
	TimeToIDName = "sf.blockmeta.v1.TimeToID"
	// BlockIDName is the fully-qualified name of the BlockID service.
	BlockIDName = "sf.blockmeta.v1.BlockID"
	// ForksName is the fully-qualified name of the Forks service.
	ForksName = "sf.blockmeta.v1.Forks"
	// ChainDiscriminatorName is the fully-qualified name of the ChainDiscriminator service.
	ChainDiscriminatorName = "sf.blockmeta.v1.ChainDiscriminator"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TimeToIDAtProcedure is the fully-qualified name of the TimeToID's At RPC.
	TimeToIDAtProcedure = "/sf.blockmeta.v1.TimeToID/At"
	// TimeToIDAfterProcedure is the fully-qualified name of the TimeToID's After RPC.
	TimeToIDAfterProcedure = "/sf.blockmeta.v1.TimeToID/After"
	// TimeToIDBeforeProcedure is the fully-qualified name of the TimeToID's Before RPC.
	TimeToIDBeforeProcedure = "/sf.blockmeta.v1.TimeToID/Before"
	// BlockIDNumToIDProcedure is the fully-qualified name of the BlockID's NumToID RPC.
	BlockIDNumToIDProcedure = "/sf.blockmeta.v1.BlockID/NumToID"
	// BlockIDLIBIDProcedure is the fully-qualified name of the BlockID's LIBID RPC.
	BlockIDLIBIDProcedure = "/sf.blockmeta.v1.BlockID/LIBID"
	// ForksResolveProcedure is the fully-qualified name of the Forks's Resolve RPC.
	ForksResolveProcedure = "/sf.blockmeta.v1.Forks/Resolve"
	// ChainDiscriminatorInLongestChainProcedure is the fully-qualified name of the ChainDiscriminator's
	// InLongestChain RPC.
	ChainDiscriminatorInLongestChainProcedure = "/sf.blockmeta.v1.ChainDiscriminator/InLongestChain"
	// ChainDiscriminatorGetBlockInLongestChainProcedure is the fully-qualified name of the
	// ChainDiscriminator's GetBlockInLongestChain RPC.
	ChainDiscriminatorGetBlockInLongestChainProcedure = "/sf.blockmeta.v1.ChainDiscriminator/GetBlockInLongestChain"
)

// TimeToIDClient is a client for the sf.blockmeta.v1.TimeToID service.
type TimeToIDClient interface {
	At(context.Context, *connect.Request[v1.TimeRequest]) (*connect.Response[v1.BlockResponse], error)
	After(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error)
	Before(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error)
}

// NewTimeToIDClient constructs a client for the sf.blockmeta.v1.TimeToID service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTimeToIDClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TimeToIDClient {
	baseURL = strings.TrimRight(baseURL, "/")
	timeToIDMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("TimeToID").Methods()
	return &timeToIDClient{
		at: connect.NewClient[v1.TimeRequest, v1.BlockResponse](
			httpClient,
			baseURL+TimeToIDAtProcedure,
			connect.WithSchema(timeToIDMethods.ByName("At")),
			connect.WithClientOptions(opts...),
		),
		after: connect.NewClient[v1.RelativeTimeRequest, v1.BlockResponse](
			httpClient,
			baseURL+TimeToIDAfterProcedure,
			connect.WithSchema(timeToIDMethods.ByName("After")),
			connect.WithClientOptions(opts...),
		),
		before: connect.NewClient[v1.RelativeTimeRequest, v1.BlockResponse](
			httpClient,
			baseURL+TimeToIDBeforeProcedure,
			connect.WithSchema(timeToIDMethods.ByName("Before")),
			connect.WithClientOptions(opts...),
		),
	}
}

// timeToIDClient implements TimeToIDClient.
type timeToIDClient struct {
	at     *connect.Client[v1.TimeRequest, v1.BlockResponse]
	after  *connect.Client[v1.RelativeTimeRequest, v1.BlockResponse]
	before *connect.Client[v1.RelativeTimeRequest, v1.BlockResponse]
}

// At calls sf.blockmeta.v1.TimeToID.At.
func (c *timeToIDClient) At(ctx context.Context, req *connect.Request[v1.TimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return c.at.CallUnary(ctx, req)
}

// After calls sf.blockmeta.v1.TimeToID.After.
func (c *timeToIDClient) After(ctx context.Context, req *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return c.after.CallUnary(ctx, req)
}

// Before calls sf.blockmeta.v1.TimeToID.Before.
func (c *timeToIDClient) Before(ctx context.Context, req *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return c.before.CallUnary(ctx, req)
}

// TimeToIDHandler is an implementation of the sf.blockmeta.v1.TimeToID service.
type TimeToIDHandler interface {
	At(context.Context, *connect.Request[v1.TimeRequest]) (*connect.Response[v1.BlockResponse], error)
	After(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error)
	Before(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error)
}

// NewTimeToIDHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTimeToIDHandler(svc TimeToIDHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	timeToIDMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("TimeToID").Methods()
	timeToIDAtHandler := connect.NewUnaryHandler(
		TimeToIDAtProcedure,
		svc.At,
		connect.WithSchema(timeToIDMethods.ByName("At")),
		connect.WithHandlerOptions(opts...),
	)
	timeToIDAfterHandler := connect.NewUnaryHandler(
		TimeToIDAfterProcedure,
		svc.After,
		connect.WithSchema(timeToIDMethods.ByName("After")),
		connect.WithHandlerOptions(opts...),
	)
	timeToIDBeforeHandler := connect.NewUnaryHandler(
		TimeToIDBeforeProcedure,
		svc.Before,
		connect.WithSchema(timeToIDMethods.ByName("Before")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.blockmeta.v1.TimeToID/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TimeToIDAtProcedure:
			timeToIDAtHandler.ServeHTTP(w, r)
		case TimeToIDAfterProcedure:
			timeToIDAfterHandler.ServeHTTP(w, r)
		case TimeToIDBeforeProcedure:
			timeToIDBeforeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTimeToIDHandler returns CodeUnimplemented from all methods.
type UnimplementedTimeToIDHandler struct{}

func (UnimplementedTimeToIDHandler) At(context.Context, *connect.Request[v1.TimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.TimeToID.At is not implemented"))
}

func (UnimplementedTimeToIDHandler) After(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.TimeToID.After is not implemented"))
}

func (UnimplementedTimeToIDHandler) Before(context.Context, *connect.Request[v1.RelativeTimeRequest]) (*connect.Response[v1.BlockResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.TimeToID.Before is not implemented"))
}

// BlockIDClient is a client for the sf.blockmeta.v1.BlockID service.
type BlockIDClient interface {
	NumToID(context.Context, *connect.Request[v1.NumToIDRequest]) (*connect.Response[v1.BlockIDResponse], error)
	// Query the CURRENT irreversible block
	LIBID(context.Context, *connect.Request[v1.LIBRequest]) (*connect.Response[v1.BlockIDResponse], error)
}

// NewBlockIDClient constructs a client for the sf.blockmeta.v1.BlockID service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewBlockIDClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) BlockIDClient {
	baseURL = strings.TrimRight(baseURL, "/")
	blockIDMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("BlockID").Methods()
	return &blockIDClient{
		numToID: connect.NewClient[v1.NumToIDRequest, v1.BlockIDResponse](
			httpClient,
			baseURL+BlockIDNumToIDProcedure,
			connect.WithSchema(blockIDMethods.ByName("NumToID")),
			connect.WithClientOptions(opts...),
		),
		lIBID: connect.NewClient[v1.LIBRequest, v1.BlockIDResponse](
			httpClient,
			baseURL+BlockIDLIBIDProcedure,
			connect.WithSchema(blockIDMethods.ByName("LIBID")),
			connect.WithClientOptions(opts...),
		),
	}
}

// blockIDClient implements BlockIDClient.
type blockIDClient struct {
	numToID *connect.Client[v1.NumToIDRequest, v1.BlockIDResponse]
	lIBID   *connect.Client[v1.LIBRequest, v1.BlockIDResponse]
}

// NumToID calls sf.blockmeta.v1.BlockID.NumToID.
func (c *blockIDClient) NumToID(ctx context.Context, req *connect.Request[v1.NumToIDRequest]) (*connect.Response[v1.BlockIDResponse], error) {
	return c.numToID.CallUnary(ctx, req)
}

// LIBID calls sf.blockmeta.v1.BlockID.LIBID.
func (c *blockIDClient) LIBID(ctx context.Context, req *connect.Request[v1.LIBRequest]) (*connect.Response[v1.BlockIDResponse], error) {
	return c.lIBID.CallUnary(ctx, req)
}

// BlockIDHandler is an implementation of the sf.blockmeta.v1.BlockID service.
type BlockIDHandler interface {
	NumToID(context.Context, *connect.Request[v1.NumToIDRequest]) (*connect.Response[v1.BlockIDResponse], error)
	// Query the CURRENT irreversible block
	LIBID(context.Context, *connect.Request[v1.LIBRequest]) (*connect.Response[v1.BlockIDResponse], error)
}

// NewBlockIDHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewBlockIDHandler(svc BlockIDHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	blockIDMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("BlockID").Methods()
	blockIDNumToIDHandler := connect.NewUnaryHandler(
		BlockIDNumToIDProcedure,
		svc.NumToID,
		connect.WithSchema(blockIDMethods.ByName("NumToID")),
		connect.WithHandlerOptions(opts...),
	)
	blockIDLIBIDHandler := connect.NewUnaryHandler(
		BlockIDLIBIDProcedure,
		svc.LIBID,
		connect.WithSchema(blockIDMethods.ByName("LIBID")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.blockmeta.v1.BlockID/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case BlockIDNumToIDProcedure:
			blockIDNumToIDHandler.ServeHTTP(w, r)
		case BlockIDLIBIDProcedure:
			blockIDLIBIDHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedBlockIDHandler returns CodeUnimplemented from all methods.
type UnimplementedBlockIDHandler struct{}

func (UnimplementedBlockIDHandler) NumToID(context.Context, *connect.Request[v1.NumToIDRequest]) (*connect.Response[v1.BlockIDResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.BlockID.NumToID is not implemented"))
}

func (UnimplementedBlockIDHandler) LIBID(context.Context, *connect.Request[v1.LIBRequest]) (*connect.Response[v1.BlockIDResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.BlockID.LIBID is not implemented"))
}

// ForksClient is a client for the sf.blockmeta.v1.Forks service.
type ForksClient interface {
	Resolve(context.Context, *connect.Request[v1.ForkResolveRequest]) (*connect.Response[v1.ForkResolveResponse], error)
}

// NewForksClient constructs a client for the sf.blockmeta.v1.Forks service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewForksClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ForksClient {
	baseURL = strings.TrimRight(baseURL, "/")
	forksMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("Forks").Methods()
	return &forksClient{
		resolve: connect.NewClient[v1.ForkResolveRequest, v1.ForkResolveResponse](
			httpClient,
			baseURL+ForksResolveProcedure,
			connect.WithSchema(forksMethods.ByName("Resolve")),
			connect.WithClientOptions(opts...),
		),
	}
}

// forksClient implements ForksClient.
type forksClient struct {
	resolve *connect.Client[v1.ForkResolveRequest, v1.ForkResolveResponse]
}

// Resolve calls sf.blockmeta.v1.Forks.Resolve.
func (c *forksClient) Resolve(ctx context.Context, req *connect.Request[v1.ForkResolveRequest]) (*connect.Response[v1.ForkResolveResponse], error) {
	return c.resolve.CallUnary(ctx, req)
}

// ForksHandler is an implementation of the sf.blockmeta.v1.Forks service.
type ForksHandler interface {
	Resolve(context.Context, *connect.Request[v1.ForkResolveRequest]) (*connect.Response[v1.ForkResolveResponse], error)
}

// NewForksHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewForksHandler(svc ForksHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	forksMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("Forks").Methods()
	forksResolveHandler := connect.NewUnaryHandler(
		ForksResolveProcedure,
		svc.Resolve,
		connect.WithSchema(forksMethods.ByName("Resolve")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.blockmeta.v1.Forks/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ForksResolveProcedure:
			forksResolveHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedForksHandler returns CodeUnimplemented from all methods.
type UnimplementedForksHandler struct{}

func (UnimplementedForksHandler) Resolve(context.Context, *connect.Request[v1.ForkResolveRequest]) (*connect.Response[v1.ForkResolveResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.Forks.Resolve is not implemented"))
}

// ChainDiscriminatorClient is a client for the sf.blockmeta.v1.ChainDiscriminator service.
type ChainDiscriminatorClient interface {
	InLongestChain(context.Context, *connect.Request[v1.InLongestChainRequest]) (*connect.Response[v1.InLongestChainResponse], error)
	GetBlockInLongestChain(context.Context, *connect.Request[v1.GetBlockInLongestChainRequest]) (*connect.Response[v1.GetBlockInLongestChainResponse], error)
}

// NewChainDiscriminatorClient constructs a client for the sf.blockmeta.v1.ChainDiscriminator
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewChainDiscriminatorClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ChainDiscriminatorClient {
	baseURL = strings.TrimRight(baseURL, "/")
	chainDiscriminatorMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("ChainDiscriminator").Methods()
	return &chainDiscriminatorClient{
		inLongestChain: connect.NewClient[v1.InLongestChainRequest, v1.InLongestChainResponse](
			httpClient,
			baseURL+ChainDiscriminatorInLongestChainProcedure,
			connect.WithSchema(chainDiscriminatorMethods.ByName("InLongestChain")),
			connect.WithClientOptions(opts...),
		),
		getBlockInLongestChain: connect.NewClient[v1.GetBlockInLongestChainRequest, v1.GetBlockInLongestChainResponse](
			httpClient,
			baseURL+ChainDiscriminatorGetBlockInLongestChainProcedure,
			connect.WithSchema(chainDiscriminatorMethods.ByName("GetBlockInLongestChain")),
			connect.WithClientOptions(opts...),
		),
	}
}

// chainDiscriminatorClient implements ChainDiscriminatorClient.
type chainDiscriminatorClient struct {
	inLongestChain         *connect.Client[v1.InLongestChainRequest, v1.InLongestChainResponse]
	getBlockInLongestChain *connect.Client[v1.GetBlockInLongestChainRequest, v1.GetBlockInLongestChainResponse]
}

// InLongestChain calls sf.blockmeta.v1.ChainDiscriminator.InLongestChain.
func (c *chainDiscriminatorClient) InLongestChain(ctx context.Context, req *connect.Request[v1.InLongestChainRequest]) (*connect.Response[v1.InLongestChainResponse], error) {
	return c.inLongestChain.CallUnary(ctx, req)
}

// GetBlockInLongestChain calls sf.blockmeta.v1.ChainDiscriminator.GetBlockInLongestChain.
func (c *chainDiscriminatorClient) GetBlockInLongestChain(ctx context.Context, req *connect.Request[v1.GetBlockInLongestChainRequest]) (*connect.Response[v1.GetBlockInLongestChainResponse], error) {
	return c.getBlockInLongestChain.CallUnary(ctx, req)
}

// ChainDiscriminatorHandler is an implementation of the sf.blockmeta.v1.ChainDiscriminator service.
type ChainDiscriminatorHandler interface {
	InLongestChain(context.Context, *connect.Request[v1.InLongestChainRequest]) (*connect.Response[v1.InLongestChainResponse], error)
	GetBlockInLongestChain(context.Context, *connect.Request[v1.GetBlockInLongestChainRequest]) (*connect.Response[v1.GetBlockInLongestChainResponse], error)
}

// NewChainDiscriminatorHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewChainDiscriminatorHandler(svc ChainDiscriminatorHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	chainDiscriminatorMethods := v1.File_sf_blockmeta_v1_blockmeta_proto.Services().ByName("ChainDiscriminator").Methods()
	chainDiscriminatorInLongestChainHandler := connect.NewUnaryHandler(
		ChainDiscriminatorInLongestChainProcedure,
		svc.InLongestChain,
		connect.WithSchema(chainDiscriminatorMethods.ByName("InLongestChain")),
		connect.WithHandlerOptions(opts...),
	)
	chainDiscriminatorGetBlockInLongestChainHandler := connect.NewUnaryHandler(
		ChainDiscriminatorGetBlockInLongestChainProcedure,
		svc.GetBlockInLongestChain,
		connect.WithSchema(chainDiscriminatorMethods.ByName("GetBlockInLongestChain")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sf.blockmeta.v1.ChainDiscriminator/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ChainDiscriminatorInLongestChainProcedure:
			chainDiscriminatorInLongestChainHandler.ServeHTTP(w, r)
		case ChainDiscriminatorGetBlockInLongestChainProcedure:
			chainDiscriminatorGetBlockInLongestChainHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedChainDiscriminatorHandler returns CodeUnimplemented from all methods.
type UnimplementedChainDiscriminatorHandler struct{}

func (UnimplementedChainDiscriminatorHandler) InLongestChain(context.Context, *connect.Request[v1.InLongestChainRequest]) (*connect.Response[v1.InLongestChainResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.ChainDiscriminator.InLongestChain is not implemented"))
}

func (UnimplementedChainDiscriminatorHandler) GetBlockInLongestChain(context.Context, *connect.Request[v1.GetBlockInLongestChainRequest]) (*connect.Response[v1.GetBlockInLongestChainResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("sf.blockmeta.v1.ChainDiscriminator.GetBlockInLongestChain is not implemented"))
}
